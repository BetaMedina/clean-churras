const { contentType } = require('./content-type')
const { cors } = require('./cors')
const { expressJson } = require('./express-json')

module.exports = {
  contentType,
  cors,
  expressJson
}
